#include "bits/stdc++.h"
using namespace std;

int main() {
  ios::sync_with_stdio(0); cin.tie(0);
  int n, q; cin >> n >> q;
  vector<int> a(n); for (int &x : a) cin >> x;
  while (q--) {
    int o, x, y; cin >> o >> x >> y;
    if (o == 1) {
    } else {
    }
  }
}
